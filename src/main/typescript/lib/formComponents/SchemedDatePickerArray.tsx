import * as React from "react"
import asFieldArray from "./FieldArrayHOC"

interface SchemedDatePickerArrayProps {

}

const SchemedDatePickerArray = (props: SchemedDatePickerArrayProps) => (
    <h1>SchemedDatePickerArray</h1>
)

export default asFieldArray(SchemedDatePickerArray)
